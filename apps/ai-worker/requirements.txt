# ============================================================
#  apps/ai-worker/requirements.txt
#  Python-Abhängigkeiten für den AI Worker
#  PyTorch wird separat via Dockerfile installiert (CUDA 12.4)
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.8.0
python-multipart==0.0.9

# ── Bildverarbeitung ─────────────────────────────────────────
Pillow==10.4.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
torchvision==0.20.1

# ── KI-Frameworks ────────────────────────────────────────────
transformers==4.46.0
diffusers==0.31.0
accelerate==1.0.0
safetensors==0.4.5
huggingface-hub==0.25.0
tokenizers==0.20.0
sentencepiece==0.2.0

# ── Character Recognition ────────────────────────────────────
insightface==0.7.3
onnxruntime-gpu==1.19.0

# ── CLIP / Style Embeddings ──────────────────────────────────
open-clip-torch==2.26.1

# ── Vektor-Datenbank ─────────────────────────────────────────
qdrant-client==1.12.0

# ── Sprachsynthese ───────────────────────────────────────────
TTS==0.22.0          # XTTS-v2
soundfile==0.12.1
librosa==0.10.2

# ── Video / Audio ────────────────────────────────────────────
imageio==2.35.0
imageio-ffmpeg==0.5.1

# ── HTTP Client ──────────────────────────────────────────────
httpx==0.27.0
aiohttp==3.10.0

# ── Cloud-Provider-Modus (AI_PROVIDER=cloud) ─────────────────
# Werden nur tatsächlich genutzt, wenn AI_PROVIDER=cloud gesetzt ist.
# Schlank genug, dass sie auch im lokalen Modus nicht stören.
# (replicate-python und anthropic SDK optional — providers/*.py
#  nutzt rohes httpx, daher hier nicht zwingend erforderlich)

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
loguru==0.7.2
tqdm==4.66.5
einops==0.8.0        # für viele Diffusion-Modelle
xformers==0.0.28     # Memory-effiziente Attention
